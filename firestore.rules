rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // WARNING: OPEN rules (no login). Anyone who knows your project ID can read/write.
    // Consider tightening later (App Check, auth, or write validation).
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
